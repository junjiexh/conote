# Development Dockerfile for hot reload
FROM node:18-alpine

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy the rest of the application
COPY . .

# Expose Vite dev server port
EXPOSE 3000

# Start development server (host config is in vite.config.js)
CMD ["npm", "run", "dev"]
